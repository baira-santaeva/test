
<svg
  xmlns="http://www.w3.org/2000/svg"
  width="300" height="160" viewBox="0 0 300 160"
  onload="console.log('SVG loaded (no XML declaration)')"
>
  <script type="application/ecmascript"><![CDATA[
    console.log('Inline <script> executed inside SVG');
    function handleCircleClick(evt) {
      const cx = evt.target.getAttribute('cx');
      const cy = evt.target.getAttribute('cy');
      console.log('Circle clicked at:', { cx, cy, time: new Date().toISOString() });
    }
  ]]></script>

  <rect x="0" y="0" width="300" height="160" fill="#0b3d91" />
  <text x="20" y="35" fill="white" font-family="sans-serif" font-size="16">
    Click the circle to log to console
  </text>
  <circle cx="150" cy="95" r="40" fill="#ffcc00" stroke="#333" stroke-width="2"
          onclick="handleCircleClick(evt)" />
</svg>
